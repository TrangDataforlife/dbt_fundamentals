models:
  - name: stg_jaffleshop_customer
    columns:
      - name: customer_id
        data_tests:
          - unique
          - not_null

  - name: stg_jaffleshop_order
    columns:
      - name: order_id
        data_tests:
          - unique
          - not_null
      - name: status
        data_tests:
          - accepted_values:
              arguments:
                values: ['placed', 'shipped', 'completed', 'returned', 'return_pending']
      - name: customer_id
        data_tests:
          - relationships:
              arguments:
                to: ref('stg_jaffleshop_customer')
                field: customer_id